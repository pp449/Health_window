//global
const HOME = "/";
const SIGNIN = "/signin";
const SIGNUP = "/signup";
const LOGOUT = "/logout";
const CHANGE_PASSWORD = "/change-password";

const routes = {
  home: HOME,
  signin: SIGNIN,
  signup: SIGNUP,
  logout: LOGOUT,
  changePassword: CHANGE_PASSWORD,
};

export default routes;
